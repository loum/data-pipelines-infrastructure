# Ignore everything in this directory as we only need it for scafolding.
# This file can be safely removed once we actually place tracked content within the directory.
*
# Except this file
!.gitignore
